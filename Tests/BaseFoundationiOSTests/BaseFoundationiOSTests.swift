import XCTest
@testable import BaseFoundationiOS

final class BaseFoundationiOSTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(BaseFoundationiOS().text, "Hello, World!")
    }
}
